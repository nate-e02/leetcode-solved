class Solution:
    def firstMissingPositive(self, nums: List[int]) -> int:
        num_set = set(nums)
        for i in range(1, 2147483647, 1):
            if i not in num_set:
                break   
        return i
            
